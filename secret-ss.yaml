apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  labels:
    app: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template: 
    metadata:
      name: mysql-temp
      labels:
        app: myapp
    spec:
      containers:
        - name: mysql-cont
          image: mysql:8.0
          ports:
            - name: mysql-port
              containerPort: 3306
          env:
            - name: MYSQL_USER
              valueFrom: 
                secretKeyRef:
                  name: my-secret
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom: 
                secretKeyRef:
                  name: my-secret
                  key: MYSQL_PASSWORD
            - name: MYSQL_DATABASE
              valueFrom: 
                secretKeyRef:
                  name: my-secret
                  key: MYSQL_DATABASE
            - name: MYSQL_ROOT_PASSWORD
              valueFrom: 
                secretKeyRef:
                  name: my-secret
                  key: MYSQL_ROOT_PASSWORD
---
apiVersion: v1
kind: Service
metadata: 
  name: mysql-svc
  labels: 
    app: myapp
spec:
  clusterIP: None
  selector:
    app: myapp
  ports:
    - name: mysql-svc-port
      protocol: TCP
      port: 3306
      targetPort: 3306