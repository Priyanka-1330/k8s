apiVersion: v1
kind: Service
metadata:
  name: my-svc
  labels:
    app : myapp
  namespace: hpa
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - name: svc-port
      port: 80
      targetPort: 80